---
import BaseLayout from './BaseLayout.astro';
import Breadcrumb from '../components/common/Breadcrumb.astro';
import { t, type Locale } from '../i18n/ui';

interface Props {
  title: string;
  description: string;
  subtitle?: string;
  tools?: string[];
  image?: string;
  locale?: Locale;
  prev?: { title: string; slug: string } | null;
  next?: { title: string; slug: string } | null;
}

const {
  title,
  description,
  subtitle,
  tools = [],
  image,
  locale = 'ko',
  prev = null,
  next = null,
} = Astro.props;

const prefix = locale === 'en' ? '/en' : '';
const researchHref = `${prefix}/research/`;

const crumbs = [
  { label: t(locale, 'common.breadcrumb_home'), href: locale === 'en' ? '/en/' : '/' },
  { label: t(locale, 'nav.research'), href: researchHref },
  { label: title },
];

const pageTitle = `${title} | ${t(locale, 'site.title')}`;
---

<BaseLayout title={pageTitle} description={description} image={image} locale={locale}>
  <article class="pb-24 lg:pb-32">
    <!-- Project header -->
    <header class="page-hero py-20 lg:py-28">
      <div class="container relative z-10">
        <Breadcrumb items={crumbs} />

        <!-- Subtitle badge -->
        {
          subtitle && (
            <span class="inline-flex items-center mt-5 px-3.5 py-1.5 text-xs font-semibold rounded-full bg-white/10 text-accent tracking-wide uppercase">
              {subtitle}
            </span>
          )
        }

        <h1 class="mt-4 text-3xl lg:text-[2.75rem] font-bold leading-tight">
          {title}
        </h1>

        <p class="mt-5 max-w-2xl text-base lg:text-lg leading-relaxed">
          {description}
        </p>

        <!-- Tools -->
        {
          tools.length > 0 && (
            <div class="flex flex-wrap gap-2 mt-6">
              {tools.map((tool) => (
                <span class="inline-flex items-center px-3 py-1.5 text-xs font-medium rounded-lg bg-white/10 text-slate-300 border border-white/10 backdrop-blur-sm">
                  {tool}
                </span>
              ))}
            </div>
          )
        }
      </div>
    </header>

    <!-- Hero image -->
    {
      image && (
        <div class="container -mt-6 relative z-10">
          <img
            src={image}
            alt={title}
            class="w-full max-h-[480px] object-cover rounded-2xl shadow-lg ring-1 ring-black/5 dark:ring-white/5"
            loading="eager"
          />
        </div>
      )
    }

    <!-- Project body -->
    <div class="container mt-16 lg:mt-24">
      <div class="max-w-3xl mx-auto">
        <div class="prose prose-lg max-w-none">
          <slot />
        </div>

        <!-- Project navigation -->
        {
          (prev || next) && (
            <nav
              class="mt-14 pt-8 border-t border-border dark:border-white/[0.08] grid grid-cols-1 sm:grid-cols-2 gap-4"
              aria-label="Project navigation"
            >
              {prev && (
                <a
                  href={`${researchHref}${prev.slug}/`}
                  class="group flex flex-col gap-1.5 p-5 rounded-xl border border-border dark:border-white/10 hover:border-accent hover:shadow-sm transition-all duration-200"
                >
                  <span class="text-xs text-muted flex items-center gap-1">
                    <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" /></svg>
                    {t(locale, 'common.prev')}
                  </span>
                  <span class="text-sm font-medium text-deep dark:text-white group-hover:text-accent transition-colors line-clamp-2">
                    {prev.title}
                  </span>
                </a>
              )}
              {next && (
                <a
                  href={`${researchHref}${next.slug}/`}
                  class="group flex flex-col gap-1.5 p-5 rounded-xl border border-border dark:border-white/10 hover:border-accent hover:shadow-sm transition-all duration-200 sm:text-right sm:col-start-2"
                >
                  <span class="text-xs text-muted flex items-center gap-1 sm:justify-end">
                    {t(locale, 'common.next')}
                    <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" /></svg>
                  </span>
                  <span class="text-sm font-medium text-deep dark:text-white group-hover:text-accent transition-colors line-clamp-2">
                    {next.title}
                  </span>
                </a>
              )}
            </nav>
          )
        }
      </div>
    </div>
  </article>
</BaseLayout>
